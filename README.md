# PWM CAR MECHANICS

This projects has 2 things:

- PWM lessons generated by matt's /teach skill
- A presentation generated using slidev based on the same content

## GOAL

The goal is create a website with this content
We need to create a landing page where you can navigate either to lessons or presentation.

### Lessons

Are not interconnected right now, so user need to be able to navigate through every page and either go back to index, need to be created as well.

Some diagrams are not well-aligned, need to get fixed as well.

Create a proper setup for client side application, or either static page generated website.

### Presentation 

Same for this, diagrams are not aligned, so they need to be aligned, also migrate components to react if needed, so we can use them in both website and presentation.